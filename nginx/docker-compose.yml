version: '3.7'

services:
  nginx:
    image: nginx:latest
    container_name: nginx
    volumes:
      - ./nginx-conf:/etc/nginx/conf.d
    ports:
      - "80:80"
      - "443:443"
    networks:
      - infra
    restart: always

networks:
  infra:
    external: true
